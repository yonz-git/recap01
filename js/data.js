// const bookmarkIcons = document.querySelectorAll('[data-js="bookmarkIcon"]');

// bookmarkIcons.forEach((icon) => {
//   icon.addEventListener("click", () => {
//     // 1. Grab the ID from the parent div
//     const cardId = icon.closest(".question-box").getAttribute("data-id");

//     // 2. Toggle the visual "filled" look
//     icon.classList.toggle("active");

//     // 3. Save to memory (Simplified example)
//     console.log(`Saved card ${cardId} to bookmarks!`);
//   });
// });
